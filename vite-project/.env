# ─── DvBakes Frontend – LOCAL DEVELOPMENT ───────────────────────────────────
# Vite proxy (vite.config.js) forwards /api/* → http://localhost:8080
# So VITE_API_URL should be EMPTY here — all calls go through the proxy.
VITE_API_URL=
